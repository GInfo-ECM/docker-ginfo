version: '2'

services:
  db:
    image: mysql:5.7
    volumes:
    - db_data:/var/lib/mysql
    restart: always
    networks:
    - dmz
    environment:
      MYSQL_ROOT_PASSWORD: dbtest
      MYSQL_DATABASE: phpginfo
      MYSQL_USER: phpginfo
      MYSQL_PASSWORD: phpginfo

  site:
    build: php-apache
    volumes:
    - site_data:/var/www
    external_links:
    - reverse-proxy:cnginx
    expose:
    - 80
    restart: always
    networks:
    - dmz
    - proxy
    environment:
      VIRTUAL_HOST: ${VIRTUAL_HOST}
      DB_HOST: db:3306
      DB_USER: phpginfo
      DB_PASSWORD: phpginfo
      LETSENCRYPT_HOST: ${VIRTUAL_HOST}
      LETSENCRYPT_EMAIL: ${EMAIL_FOR_CERTIFICATION}
      TRUSTED_PROXIES: cnginx

volumes:
  db_data:
    name: db_data
  site_data:
    name: site_data


networks:
  proxy:
    external:
      name: nginx-proxy-test